<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact Form</title>
</head>
<body>

<header class="text-center">
    <h1>Contact Us for a Free Consultation</h1>
    <p>Our availability is from 9 AM to 5 PM, Monday to Friday. Reach out to us via phone or email!</p>
  </header>


<div class="container">
    <!-- Your contact form goes here -->
    <%= form_with(url: '/contacts', method: 'post') do |form| %>
      <div class="form-group">
        <%= form.label :name, 'Your Name:' %>
        <%= form.text_field :name, class: 'form-control', required: true %>
      </div>

      <div class="form-group">
        <%= form.label :email, 'Your Email:' %>
        <%= form.email_field :email, class: 'form-control', required: true %>
      </div>

      <div class="form-group">
        <%= form.label :phone, 'Your Phone Number:' %>
        <%= form.text_field :phone, class: 'form-control' %>
      </div>

       <div class="form-group">
        <%= form.label :service_type, 'Select Service:' %>
        <%= form.select :service_type, ['Frontend Design', 'E-commerce Website', 'Full Website', 'Graphic Design/Logo Design', 'Backend API & Design'], {}, { class: 'form-control', required: true } %>
      </div>

      <div class="form-group">
        <%= form.label :message, 'Your Message:' %>
        <%= form.text_area :message, class: 'form-control', rows: 4, required: true %>
      </div>

      <div class="form-group">
        <%= form.submit 'Submit', class: 'btn btn-primary' %>
      </div>
      <p>Your privacy is protected and we hate spam.</p>
    <% end %>
  </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact Form</title>
</head>
<body>

<header class="text-center">
    <h1>Contact Us for a Free Consultation</h1>
    <P>Our availability is from 9 AM to 5 PM, Monday to Friday. Reach out to us via phone or email!</p>
  </header>


<div class="container">

<% if flash[:error] %>
  <div class="alert alert-danger">
    <strong>Error:</strong> <%= flash[:error] %>
    <ul>
      <% flash[:error_messages].each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<% if flash[:success] %>
  <div class="alert alert-success">
    <%= flash[:success] %>
  </div>
<% end %>
       <!-- Your contact form goes here -->
       <script>

       alert(<%= @contact %>)
       </script>
    <%= form_with(model: @contact, url: send_contact_path, method: 'post') do |form| %>
      <div class="form-group">
        <%= form.label :name, 'Your Name:' %>
        <%= form.text_field :name, class: 'form-control', required: true %>
      </div>

      <div class="form-group">
        <%= form.label :email, 'Your Email:' %>
        <%= form.email_field :email, class: 'form-control', required: true %>
      </div>

      <div class="form-group">
        <%= form.label :phonenumber, 'Your Phone Number:' %>
        <%= form.text_field :phonenumber, class: 'form-control' %>
      </div>

       <div class="form-group">
        <%= form.label :select_service_type, 'Select Service:' %>
        <%= form.select :select_service_type, ['Frontend Design', 'E-commerce Website', 'Full Website', 'Graphic Design/Logo Design', 'Backend API & Design'], {}, { class: 'form-control', required: true } %>
      </div>

      <div class="form-group">
        <%= form.label :message, 'Your Message:' %>
        <%= form.text_area :message, class: 'form-control', rows: 4, required: true %>
      </div>

      <div class="form-group">
        <%= form.submit 'Submit', class: 'btn btn-primary' %>
      </div>
      <p>Your privacy is protected and we hate spam.</p>
    <% end %>
  </div>
</body>
</html>
